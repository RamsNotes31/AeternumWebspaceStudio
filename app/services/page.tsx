import { PageHero } from "@/components/sections/page-hero";

export default function ServicesPage() {
  return (
    <PageHero
      eyebrow="Services"
      title="Detail layanan Aeternum akan menjelaskan output, scope, dan cocok untuk siapa."
      description="Halaman ini disiapkan untuk service cards lengkap: website company profile, landing page, web app internal, inventory system, admin dashboard, backend API, database design, deployment, dan maintenance."
    />
  );
}
